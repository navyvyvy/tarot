{
  "name": "noru-tarot",
  "private": true,
  "scripts": {
    "test": "node --test",
    "check": "node --check app.js && node --check core.js && node --check card.js && node --check locale/ko.js && node --check sw.js"
  }
}
